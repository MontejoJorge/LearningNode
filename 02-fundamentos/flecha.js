// function sumar(a,b) {
//     return a + b;
// }
const sumar = (a,b) => {
    return a + b;
}

//si la funcion solo tiene un return se puede simplificar asi 
const restar = (a,b) => a - b;

console.log(sumar(4,4));
console.log(restar(4,2));